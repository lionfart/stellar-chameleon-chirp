import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.stellarchameleonchirp',
  appName: 'stellar-chameleon-chirp',
  webDir: 'dist'
};

export default config;
